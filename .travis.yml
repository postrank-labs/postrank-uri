language: ruby
rvm:
  - 2.1
  - 2.2
  - 2.3
  - 2.4
before_install:
  - gem install bundler
install:
  - bundle install
script:
  - bundle exec rake
  - bundle exec appraisal install
  - bundle exec rake appraisal
